server.port=8080

spring.application.name=utimea
spring.datasource.url=jdbc:postgresql://ep-fancy-term-a1x2oc0t-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_g1yCK9qNVclw
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

eureka.client.enabled=${EUREKA_ENABLED:true}
eureka.client.service-url.defaultZone=${EUREKA_SERVER_URL:http://localhost:8761/eureka/}
eureka.client.fetch-registry=true
eureka.client.register-with-eureka=true

# Excel processing configuration
# Set to 0 to auto-calculate based on CPU cores
excel.thread-pool.core-size=0
excel.thread-pool.max-size=0
excel.batch-size=0

# Turn off the logging that wraps SQL parameters
logging.level.org.hibernate.SQL=ERROR
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=ERROR

# Keep your app logs visible
logging.level.org.uit.utimea=INFO

# Notification service configuration
notification.service.url=${NOTIFICATION_SERVICE_URL:http://localhost:8081}

# Default passwords for imported users
user.default.password.teacher=${TEACHER_DEFAULT_PASSWORD:uit@teacherPsw}
user.default.password.student=${STUDENT_DEFAULT_PASSWORD:uti@studentPsw}

# JWT Configuration
jwt.secret=${JWT_SECRET:utimea-secret-key-for-jwt-token-generation-minimum-256-bits-required-for-hmac-sha-algorithms}
jwt.expiration=${JWT_EXPIRATION:86400000}

# Redis Configuration
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.password=${REDIS_PASSWORD:}
spring.data.redis.timeout=2000ms

# Email Configuration
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=utimea.uit@gmail.com
spring.mail.password=xwpa hozr ybet bexx
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# OTP Configuration
otp.expiration.minutes=${OTP_EXPIRATION_MINUTES:10}
otp.length=${OTP_LENGTH:6}